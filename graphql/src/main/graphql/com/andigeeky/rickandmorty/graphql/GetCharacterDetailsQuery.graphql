query GetCharacterDetails($id: ID!){
    character(id: $id){
        id
        name
        status
        image
        species
        type
        gender
        origin{
            id
            name
        }
        location{
            id
            name
        }
        episode{
            id
            name
            air_date
        }
    }
}